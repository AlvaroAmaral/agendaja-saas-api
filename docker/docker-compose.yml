services:
  db:
    image: postgres:15
    container_name: agendaja-db
    environment:
      POSTGRES_USER: alvaro
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: agendaja_db
    ports:
      - "5432:5432"